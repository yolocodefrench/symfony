{% extends 'base.html.twig' %}

{% block body %}
    <h1>Compterendu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteRendu.id }}</td>
            </tr>
            <tr>
                <th>Chemin</th>
                <td>{{ compteRendu.chemin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('compterendu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('compterendu_edit', { 'id': compteRendu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
